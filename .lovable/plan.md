## Диагностика
- Продавец подтвердил получение 3 позиций → заработано **4 050 ₽**.
- В таблице `payouts` есть один старый вывод на **10 800 ₽** (от 03.07.2026), сделанный ещё по старой логике, когда вывод был доступен со статуса «Доставлен» (до введения «выкупа»).
- Текущая формула: `available = max(0, earned − withdrawn) = max(0, 4050 − 10800) = 0`, поэтому кнопка «Вывести средства» задизейблена, хотя новые «выкупленные» товары есть.

## План исправления
1. Данные: удалить единственный устаревший demo-payout (`2517a0bf-e51f-4efd-9888-1cbd59428f64`), чтобы обнулить долг по старой модели. Это разовая правка данных — новых заказов она не касается.
2. Проверить в UI: после сброса «Доступно к выводу» станет 4 050 ₽, кнопка активна, вывод пройдёт. Оставшиеся 9 036 ₽ корректно останутся в «В ожидании» до подтверждения покупателем.

Кода менять не нужно — сама логика `getSellerFinance` / `requestPayout` корректная.