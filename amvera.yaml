meta:
  environment: nodejs
  toolchain:
    name: nodejs
    version: "22"

build:
  nixPacks:
    buildCmd: npm install && npm run build

run:
  persistenceMount: /data
  cmd: node .output/server/index.mjs
