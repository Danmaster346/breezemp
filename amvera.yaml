meta:
  environment: nodejs
  toolchain:
    name: node
    version: "20"

build:
  nixPacks:
    buildCmd: npm install && npm run build

run:
  persistenceMount: /data
  cmd: node .output/server/index.mjs
