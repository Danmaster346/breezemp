meta:
  environment: nodejs
  toolchain:
    name: nodejs
    version: "22"

build:
  buildCmd: npm install && NITRO_PRESET=node-server npm run build

run:
  persistenceMount: /data
  cmd: node .output/server/index.mjs
